# =============================================================================
# Ohanna-Vision
# =============================================================================

vision:
  # Enable observation export to Ohanna-Vision.
  enabled: true

  # Complete REST ingestion endpoint.
  observation_url: http://127.0.0.1:8000/api/observations

  # Maximum duration of one HTTP request.
  timeout_seconds: 5.0